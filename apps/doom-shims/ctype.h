/* <ctype.h> shim. All classifiers and case-conversion helpers live in
 * libstink.h, declared with the same ASCII-only semantics the C standard
 * spells out for the "C" locale. */
#ifndef _STINK_CTYPE_H
#define _STINK_CTYPE_H
#include "libstink.h"
#endif
